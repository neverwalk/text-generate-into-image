{
  "main": "dist/main.js",
  "scripts": {
    "build": "npx tsc && npm run build-css",
    "build-css": "sass src/assets/style.scss dist/assets/style.css",
    "watch-css": "sass --watch src/assets/style.scss:dist/assets/style.css",
    "start": "npm run build && electron ."
  },
  "dependencies": {
    "canvas": "^3.1.0",
    "electron": "^35.0.1",
    "electron-squirrel-startup": "^1.0.1",
    "sharp": "^0.33.5"
  },
  "devDependencies": {
    "@electron-forge/cli": "^7.7.0",
    "@types/node": "^22.13.10",
    "sass": "^1.85.1",
    "typescript": "^5.8.2"
  }
}
